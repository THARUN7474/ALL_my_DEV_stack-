<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Basic Note-Taking App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white">
    <div class="flex justify-center items-center min-h-screen">
        <div class="bg-zinc-800 p-6 rounded-lg shadow-lg w-full max-w-md">
            <h1 class="text-2xl font-bold mb-4">Note Taking App</h1>
            <form action="/create" method="post" class="space-y-4">
                <div>
                    <label for="title" class="block text-sm font-medium text-gray-300">Title</label>
                    <input type="text" id="title" name="title"
                        class="w-full px-3 py-2 bg-zinc-700 text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                        placeholder="Title goes here">
                </div>
                <div>
                    <label for="content" class="block text-sm font-medium text-gray-300">Content</label>
                    <textarea id="content" name="content" rows="5"
                        class="w-full px-3 py-2 bg-zinc-700 text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                        placeholder="Write your notes here..."></textarea>
                </div>
                <div>
                    <input type="submit" value="Save Note"
                        class="w-full px-3 py-2 bg-blue-600 text-white rounded-lg cursor-pointer hover:bg-blue-700">
                </div>
            </form>
        </div>

        <div class="tasks mt-8 space-y-4 w-full max-w-md">
            <% if (tasks.length> 0) { %>
                <% tasks.forEach(task=> { %>
                    <div class="task bg-zinc-800 p-4 rounded-lg shadow-lg text-white">
                        <h2 class="text-xl font-bold  text-white">
                            <%= task.heading %>
                        </h2>
                        <a href="<%= task.link %>" class="text-blue-400 hover:underline">
                            <%= task.linkText %>
                        </a>
                        <a href="<%= task.link%>/edit" class="text-yellow-400 hover:underline ml-4">Edit Note</a>
                        <p class="mt-2">
                            <%= task.description %>
                        </p>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <div class="notask bg-zinc-800 p-4 rounded-lg shadow-lg text-center">
                                No tasks yet
                            </div>
                            <% } %>
        </div>
    </div>
</body>

</html>